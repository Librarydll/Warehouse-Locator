@page
@model Warehouse.Web.Pages.MaterialOutcomes.IndexModel
@{
    ViewData["Title"] = "Material outcome list";
}
<h2>Materials outcome</h2>
<p>
    <a asp-page="Create" type="button" class="btn btn-success">Create New</a>
</p>

<table class="table table-bordered table-striped table-sm">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Outcomes[0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Outcomes[0].OutcomeDate)
            </th>
            <th>
                User
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Outcomes)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Title)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.OutcomeDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.User.FullName)
                </td>
                <td style="width:10%">
                    <a asp-page="./Edit" class="btn btn-sm btn-primary" asp-route-id="@item.Id">
                        <i class="bi bi-pencil">

                        </i>
                    </a>
                    <a asp-page="./Delete" class="btn btn-sm btn-danger" asp-route-id="@item.Id">
                        <i class="bi bi-trash">

                        </i>
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>

@{
    var prevDisabled = !Model.Outcomes.HasPreviousPage ? "disabled" : "";
    var nextDisabled = !Model.Outcomes.HasNextPage ? "disabled" : "";
}

<a asp-page="./Index"
   asp-route-pageIndex="@(Model.Outcomes.PageIndex - 1)"
   class="btn btn-primary @prevDisabled">
    Previous
</a>
<a asp-page="./Index"
   asp-route-pageIndex="@(Model.Outcomes.PageIndex + 1)"
   class="btn btn-primary @nextDisabled">
    Next
</a>
