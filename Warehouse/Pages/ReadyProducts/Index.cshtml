@page
@model Warehouse.Web.Pages.ReadyProducts.IndexModel
@{
    ViewData["Title"] = "Ready products list";
}
<h2>Ready prodcuts</h2>
<p>
    <a asp-page="Create" type="button" class="btn btn-success">Create New</a>
</p>

<table class="table table-bordered table-striped table-sm">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ReadyProducts[0].Title)
            </th>           
            <th>
                @Html.DisplayNameFor(model => model.ReadyProducts[0].Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.ReadyProducts)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Title)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td style="width:10%">
                    <a asp-page="./Edit" class="btn btn-sm btn-primary" asp-route-id="@item.Id">
                        <i class="bi bi-pencil">

                        </i>
                    </a>
                    <a asp-page="./Delete" class="btn btn-sm btn-danger" asp-route-id="@item.Id">
                        <i class="bi bi-trash">

                        </i>
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>

@{
    var prevDisabled = !Model.ReadyProducts.HasPreviousPage ? "disabled" : "";
    var nextDisabled = !Model.ReadyProducts.HasNextPage ? "disabled" : "";
}

<a asp-page="./Index"
   asp-route-pageIndex="@(Model.ReadyProducts.PageIndex - 1)"
   class="btn btn-primary @prevDisabled">
    Previous
</a>
<a asp-page="./Index"
   asp-route-pageIndex="@(Model.ReadyProducts.PageIndex + 1)"
   class="btn btn-primary @nextDisabled">
    Next
</a>