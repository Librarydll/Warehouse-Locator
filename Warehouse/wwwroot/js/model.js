export class IncomeItem {
    constructor(
        materialTitle,
        materialId,
        price,
        count)
    {
        this.materialTitle = materialTitle;
        this.count = count;
        this.materialId = materialId;
        this.price = price;
    }
}
